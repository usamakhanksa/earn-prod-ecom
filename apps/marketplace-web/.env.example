# ------------------------------------------------------------------
# GlobalMart marketplace-web — environment template. Copy to .env.local.
# ------------------------------------------------------------------

# Base URL of marketplace-api. Exposed to the browser (NEXT_PUBLIC_) since
# the shared API client runs client-side for the login/register/country
# calls built in this pass.
NEXT_PUBLIC_MARKETPLACE_API_URL=http://localhost:4100
