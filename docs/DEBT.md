# OmniSell — Technical Debt Register
> Rule (prompt.md): incomplete features ship behind a feature flag and are listed here.
> A flagged/stubbed entry must never be presented to users as real data.

| ID | Area | Debt | Flag / Guard | Planned fix |
|---|---|---|---|---|
| 0-D1 | Tooling | PowerShell/unicode npm prefix breaks global pnpm on the original dev machine | pnpm pinned as root devDependency (`node_modules/.bin/pnpm`) | none — keep local pnpm |
| 0-D2 | Tooling | Docker is not present in the local sandbox, so the docker-compose stack, RLS application, real migrations and Testcontainers suite could not be executed here | `test:integration` guarded by containers; CI runs Postgres/Redis services | verify on a Docker-enabled machine; CI is the gate |
| 0-D3 | api | `cuid()` used instead of `cuid2()` for id defaults (Prisma 5 has no built-in `cuid2`) | schema comment | re-evaluate with Prisma 6 or a cusde adapter in a follow-up migration |
| 0-D4 | api | Enum columns are stored as free strings (validated by zod at the service layer) — DB-level CHECK constraints pending | `packages/shared` enums | DB constraints via migration in Phase 1 |
| 0-D5 | api | Redis health check + BullMQ queue placeholder (`readyz` reports redis 'degraded') | `test:integration` gated | lands with Phase 1 queue module |
| 0-D6 | web/admin | Real auth → app shell flow not implemented (Phase 1) — dashboard shows placeholder tiles | — | Phase 1 |
| 0-D7 | mobile | **CLOSED this pass (Phase 4.5).** The three placeholder screens (`apps/mobile/app/consumer/{wallet,videos,shop}.tsx`) are now wired to the real Phase 4.5 backend: `wallet.tsx` fetches `GET /v1/wallet` + `/v1/wallet/transactions` with real loading/empty/error states; `videos.tsx` lists `GET /v1/videos` and links to a real full-screen player (`app/consumer/video/[id].tsx`, new this pass) built on `expo-av` — explicit play only (no autoplay), a real 5s heartbeat loop (`POST /v1/video-watches/:id/heartbeat`) that starts on play and stops on pause/background, `POST .../complete` on natural end or a manual "Earn points" tap, and an `AccessibilityInfo.announceForAccessibility` call for earned points (the RN equivalent of a web `aria-live` region); `shop.tsx` lists `GET /v1/products` and a real preview→confirm redemption flow (`POST /v1/wallet/redeem` then `/redeem/confirm` with a real `Idempotency-Key` via `expo-crypto`'s `randomUUID`). **Precisely what "closed" means here**: real, working request/response wiring against the real API, parse-checked clean by ESLint's TypeScript parser (`pnpm --filter @omnisell/mobile exec eslint ...` — 0 errors on all 5 new/changed files) — NOT exercised on a real device/emulator/simulator (no Docker, no device in this sandbox, the same standing limitation 1-D4/2-D12/4-D12 already documented for every other mobile screen). `expo-av` and `expo-crypto` are new dependencies added this pass, likewise never run on-device. See 4.5-D4 below for the same caveat scoped to this phase's specific screens | none — real code, on-device verification is the only remaining gap (see 4.5-D4) | run on a real device/simulator before shipping; add a Detox/Maestro E2E case for start→heartbeat→complete→wallet-refresh |
| 0-D8 | connector | **CLOSED this pass (Phase 3).** Four real connector adapters now exist — Printful, Printify, Gelato, Prodigi (`packages/connectors/src/adapters/`) — with real, live-doc-confirmed auth/endpoints (see docs/CONNECTORS.md), real MSW-mocked unit tests (52 passing), and the compile-time Tier-C `publish()` boundary fixed and proven (1-D18, below). **Precisely what "exist" means here**: real code, real types, MSW-tested — NOT live-sandbox-verified against any real Printful/Printify/Gelato/Prodigi account (no credentials, no live network access to a real provider account exists in this sandbox — network access to fetch public DOCS pages is confirmed available and was used, which is a different thing from an authenticated API call). See 3-D3/3-D4 below for the precise live-verification gap this leaves open | `status: 'BETA'` (not `'ACTIVE'`) on all four registry rows records the gap honestly; nightly contract tests (`test/contract/*.contract.test.ts`) skip cleanly without real credential env vars | api-registration.md §7's full protocol (real sandbox account + call + human legal sign-off) before flipping any of the four to `'ACTIVE'` |
| 0-D9 | api-client | `types.openapi.gen.ts` is a placeholder until the API serves OpenAPI | `pnpm openapi:gen` | after Phase 1 endpoints exist |
| 0-D10 | a11y | axe gate requires a running stack (Playwright not installed here) | `E2E=1 pnpm axe` | CI accessibility-gate job |
| 0-D11 | husky | Husky could not init until the first commit exists (".git can't be found") | hooks committed manually | run `pnpm prepare` after the initial commit |
| 0-D12 | api | OTel/Sentry wiring not yet implemented (observability) | read `/v1/healthz` | Phase 0.10 follow-up; SENTRY_DSN env read from config |
| 0-D13 | api | Several Phase 0 test files used a broken relative import depth (`../../src/...` from files directly under `test/`), which would fail module resolution on an actual run despite the Phase 0 report claiming green | fixed in Phase 1 pass (`test/*.test.ts` now import via `../src/...`) | none — verify `pnpm test` stays green in CI |
| 1-D1 | api | No Prisma migration has ever been generated/applied (schema-only; Docker absent in this sandbox, same root cause as 0-D2) — **grew again this pass (Phase 2)**: `Folder`, `Asset`, `AssetVersion`, `AssetUploadSession`, `PreflightReport`, `Collection`, `CollectionAsset`, `Blueprint`, `BlueprintVariant`, `ProductVariant`, `VariantPrice`, `DesignPlacement`, `PlacementTemplate`, `PricingRule`, `MockupTemplate`, `MockupRender`, `FxRate`, plus new columns on the existing `Product` model, are schema-only additions on top of the unrun Phase 1 schema | `db:migrate`/`db:deploy` scripts exist but unrun; `infra/db/rls.sql` extended with matching tenant-isolation policies for every new tenant-scoped table (also unapplied) | run `pnpm db:migrate` + `psql -f infra/db/rls.sql` on a Docker-enabled machine, then fold the RLS SQL into the migration's `migration.sql` |
| 1-D2 | api | OAuth (Google/Apple) — **partially resolved this pass**: `OAuthService`/`OAuthController` are fully implemented against each provider's real documented OAuth 2.0 endpoints (`buildAuthUrl`, token exchange, account linking) and return an honest `501 oauth_provider_not_configured` problem+json without real credentials, which is the normal state here. Still needs real `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET`/`APPLE_*` from the product owner before it can ever be exercised end-to-end, and the id_token signature verification gap tracked separately as 1-D10 | `oauth_provider_not_configured` (501) when env vars are absent (the default) | supply real credentials, then verify the full redirect → callback → account-link flow against a live provider sandbox before enabling |
| 1-D3 | web/admin | **Resolved this pass**: org switcher + invite UI (1.6, `apps/web/app/(shell)/team`), full sidebar shell (1.7, `apps/web/components/sidebar/*`), and the admin shell (1.8, `apps/admin/app/(shell)/*`) are built and wired to real session/API state, not mock data. Most individual sidebar destinations beyond Team/Dashboard/Settings-stub and Command-Centre/Feature-Flags render an honest "coming soon" screen (`(shell)/[...slug]`) rather than a real feature screen — those are Phase 2+ scope, not a gap in this pass | `(shell)/[...slug]` catch-all | build out each real section in its own later phase |
| 1-D4 | mobile | **Resolved this pass**: bottom tabs (Home/Listings/Orders/Studio/More per featureslist.md §0.3), the "More" drawer mirroring the web sidebar tree, `expo-secure-store` token storage, and a biometric-unlock guard/toggle wired against `expo-local-authentication` are all built. Caveat: none of it has been exercised on a real device or emulator (no Docker, no device in this sandbox) — verified via `vitest` (logic) and manual code review only, not a running app. Biometric unlock specifically is real code but its actual native prompt is unverifiable here | untested-on-device; `1-D16` tracks a separate, unrelated `tsc` issue for the whole app | run on a real device/simulator before shipping; add Detox/Maestro E2E per the existing `test:e2e` placeholder |
| 1-D5 | api | **Resolved this pass**: generic `AuditLogService` + a global `AuditLogInterceptor` (`APP_INTERCEPTOR`) now cover every mutating request by default. `AuthService`'s three original hand-written rows (`user.register`, `session.reuse_detected`, `user.password_reset`) were deliberately left as direct `prisma.auditLog.create` calls rather than routed through the generic service, and their controller methods carry `@SkipAuditLog()` so the interceptor doesn't double-write a lower-fidelity row for the same event — see `AuditLogService`'s doc comment for the full reasoning. New services (MFA, Invite, FeatureFlag, Tenants/Members) follow the same "write your own precise row + `@SkipAuditLog()`" pattern for their most meaningful mutations | `@SkipAuditLog()` decorator; generic interceptor is the catch-all default | none open — see 1-D11 for the generic interceptor's inherent before/after limitation |
| 1-D6 | api | **Resolved this pass**: `FeatureFlagService`/`FeatureFlagController` (per-tenant targeting + deterministic rollout-percentage bucketing + platform-admin-gated global definitions) and `NotificationService`/`NotificationController` (in-app rows + opt-in email, list + mark-read + preferences) are both built. Realtime delivery (WebSocket/SSE push so an open tab updates without a refresh or poll) is explicitly deferred — `GET /v1/notifications` is poll-based today, and the sidebar's badge count polls every 60s per featureslist.md §0.1's own fallback wording | poll-based, not push-based | wire a WebSocket gateway or SSE stream once the realtime module lands |
| 1-D7 | api | `MailerService` points at Mailpit via env vars but delivery is unverified in this sandbox (no Docker) — failures degrade silently by design. **Grew this pass**: `InviteService`'s invite-email and `NotificationService`'s opt-in email delivery go through the same unverified path | `test/mailer.service.test.ts` only proves the graceful-degrade path, not real delivery | verify against `docker compose up mailpit` on a Docker-enabled machine |
| 1-D8 | root | `pnpm lint` reports "was not found by the project service" parsing errors across nearly every package's config/test files (`*.config.{mjs,ts,cjs,js}`, several `packages/*/test/*.test.ts`) — the typescript-eslint project-service setup in `packages/config/eslint/eslint.base.mjs` doesn't cover these paths. **Grew this pass**: the 8 new `vitest.config.ts` files added to unblock real test runs (`packages/shared`, `packages/api-client`, `packages/ui`, `packages/i18n`, `packages/connectors`, `apps/web`, `apps/admin`, `apps/mobile` — see the "environment/tooling" note in the Phase 1 follow-up report) fall into the exact same bucket, so the count rose from 22 to ~30. **Grew again this pass (Phase 2)**: one new test file, `packages/shared/test/money-math.test.ts`, falls into the identical bucket (confirmed by scoped `eslint packages/shared/src` — 0 errors — vs `eslint packages/shared/src packages/shared/test` — 4 parsing errors, one per pre-existing test file plus this new one; same for `packages/ui/test/ui.test.tsx`, which already existed and simply gained more tests). Every new *source* directory this pass touched (`apps/api/src`, `apps/web/app`+`lib`+`components`, `apps/mobile/app`+`lib`+`components`) lints clean at 0 errors when scoped separately from `test/`. **A fix was attempted and reverted in the Phase 1 pass**: setting `projectService: { allowDefaultProject: [...] }` did make the parsing errors disappear, but it made `pnpm lint` blow up node's memory (multi-GB per worker) and crash with no output instead of finishing — apparently `allowDefaultProject`'s fallback tries to build a full type-checked program per matched file, which doesn't scale here. Reverted back to plain `projectService: true` (the original, slow-but-finite behaviour); one real, unrelated a11y lint error the Phase 1 pass *did* fix directly: `apps/web/components/sidebar/sidebar.tsx`'s roving-tabindex `<nav onKeyDown>` (justified `eslint-disable-next-line`, see the file) | none — pre-existing Phase 0 ESLint setup, now visible after fixing the tsconfig-`extends` bug (0-D13) that previously masked most strict-mode issues | a real fix needs either per-package tsconfig `include` entries for these files (safer, more tedious) or a correctly-scoped `allowDefaultProject` glob tested for memory behaviour on a large monorepo before landing — do not retry the naive global-glob approach |
| 1-D9 | api | TOTP MFA secrets (`MfaSecret.secret`) are stored as plaintext columns, not envelope-encrypted via `KMS_MASTER_KEY` the way `Credential` (Connector layer, Phase 3) is specified to be in prompt.md | none | envelope-encrypt at rest in a follow-up migration, alongside whatever KMS wrapper Phase 3's connector credentials use — don't build two different envelope-encryption helpers |
| 1-D10 | api | OAuth id_token claims (`OAuthService`'s `decodeIdTokenClaims`) are decoded but **not signature-verified** against the provider's JWKS — acceptable only because the whole path is unreachable without real credentials (1-D2) and is never exercised in this sandbox | `oauth_provider_not_configured` gate makes the unverified-decode path dead code today | add JWKS signature verification before real Google/Apple credentials are ever supplied — do not flip this live without it |
| 1-D11 | api | Generic `AuditLogInterceptor` (1.10) can only supply a redacted **response snapshot** as `after` and never a real `before` — it has no per-route knowledge of entity shape. Only the hand-written call sites (`AuthService`, `MfaService`, `InviteService`, `FeatureFlagService`, `TenantsService`) have real before/after diffs | documented in `AuditLogService`'s doc comment; `@SkipAuditLog()` prevents double-writes on those routes | a route→entity registry (or a decorator carrying a "fetch current row" callback) if more services need real diffs from the generic path instead of writing their own manual call |
| 1-D12 | api | Invite acceptance requires the caller to already have an OmniSell account whose email matches the invite (`InviteService.accept`) — no anonymous "sign up straight from the invite token" flow | documented in `docs/OPEN_QUESTIONS.md` #16 | build the combined accept+register flow once product defines what happens if the invited person has no account and no password yet |
| 1-D13 | admin | The admin console's login (`apps/admin/lib/session-context.tsx`) does not support the TOTP MFA challenge step — an admin account with MFA enabled gets a clean `501 admin_mfa_unsupported` instead of a working second factor. The seeded demo `platform-admin@demo.test` account has MFA disabled, so the demo flow works end to end | `admin_mfa_unsupported` error code | wire the same challenge-token exchange the web app uses (`/auth/mfa/challenge`) into the admin login form |
| 1-D14 | admin | `/admin`'s Feature Flags & Config screen manages only the global default (`FeatureFlag`) — there is no UI yet for setting a *specific* tenant's override (`FeatureFlagTarget`), even though the API supports it (`PUT /v1/feature-flags/:key/tenants/:tenantId`) | none | add a tenant picker + override table once the admin Tenants screen (still a coming-soon stub) exists to pick a tenant from |
| 1-D15 | admin/web | `next build` fails for both `apps/web` and `apps/admin` while statically prerendering Next's own built-in `/404` error page (`TypeError: Cannot read properties of null (reading 'useRef')`), even with zero app code (confirmed by reproducing it against `apps/admin`'s nearly-untouched Phase 0 shell). Root cause: a duplicate/mismatched React instance between the workspace's hoisted `react@19.0.0` and `next@15.0.3`'s own vendored `node_modules/next/node_modules/react` — pnpm already reports `unmet peer react` warnings for `next@15.0.3` (wants `18.2` or a `19.0.0-rc`, found `19.0.0` stable), which is the same root cause surfacing twice. `next dev` is unaffected — every new page in this pass was verified via `next dev` + curl instead (see Phase 1 follow-up report) | none — `next build` currently cannot be used as a real gate for either app | pin a `next` version whose peer range matches the installed `react@19.0.0` exactly (or pin `react`/`react-dom` to whatever exact RC `next@15.0.3` expects), then re-verify `next build` on both apps before relying on it in CI |
| 1-D16 | mobile | `apps/mobile`'s `tsc --noEmit` fails on every screen that renders a `react-native`/`expo-router`/`react-native-safe-area-context` component (`'X' cannot be used as a JSX component` / `Type 'bigint' is not assignable to type 'ReactNode'`) — confirmed pre-existing by reproducing the identical error against Phase 0's original, completely untouched `(tabs)/_layout.tsx` (`<Tabs>`) and `index.tsx` (`<SafeAreaView>`). Root cause: duplicate `@types/react` copies in the pnpm graph (root hoists `19.0.0` for web/admin; `apps/mobile`, `react-native`, and `@react-native/virtualized-lists` each carry their own `18.3.12`), which TypeScript treats as nominally distinct JSX/`ReactNode` types. `vitest run` is unaffected (esbuild strips types, doesn't check them) — every new mobile file in this pass was verified that way instead. **Reproduced again this pass (Phase 2)**: the same `SafeAreaView` error now also fires on the 3 new/rewritten Studio & Catalog screens (`app/(tabs)/studio.tsx`, `app/more/catalog-products.tsx`, `app/more/catalog-products/[id].tsx`) — confirmed as the identical pre-existing defect class, not a new one, by the error text and file pattern matching exactly | none — `apps/mobile` typecheck cannot currently be used as a real gate | pin one `@types/react` per React major version tree-wide via a root `pnpm.overrides` entry (risks destabilizing web/admin's React 19 types — needs its own careful pass, not a drive-by fix) |
| 1-D17 | Tooling | `vitest run` for every package except `apps/api` (which already had its own `vitest.config.ts`) failed with `Error: Could not resolve entry for router entry: router` before this pass — Vite's config search, finding no local `vite.config.*`/`vitest.config.*`, walks up past the `ecom-prod-earn` repo root (which has no `.git` of its own — the actual git root is the *parent* `best-web-developer` folder, confirmed by `git status` reporting the whole repo as one untracked directory) and picks up an unrelated stray `vite.config.ts` (a `@tanstack/start-plugin-core` project) that happens to live in that parent folder. **Fixed this pass** by adding a minimal local `vitest.config.ts` to every package that lacked one (`packages/shared`, `packages/api-client`, `packages/ui`, `packages/i18n`, `packages/connectors`, `apps/web`, `apps/admin`, `apps/mobile`) — matching `apps/api`'s existing pattern, which stops the upward search. `apps/web`/`apps/admin`/`apps/mobile` also needed `esbuild.jsx: 'automatic'` in that same file (their tsconfigs set `"jsx": "preserve"` for Next/Expo's own compiler, which made Vite's default esbuild JSX transform fall back to the classic runtime and fail with `ReferenceError: React is not defined` in every `.tsx` test) | fixed via new `vitest.config.ts` files (this pass) | none open for `ecom-prod-earn` itself; the stray outer-folder contamination is a sandbox artifact, not something to "fix" in this repo — if this repo is ever cloned standalone (own git root, no sibling scratch projects) the same bug class could still resurface for any *new* package that forgets its own `vitest.config.ts`, so keep the convention going forward |
| 1-D18 | connector | **CLOSED this pass (Phase 3).** `packages/connectors/test/automation-boundary.test.ts` was rewritten as the canonical compile-time proof: a fully-populated `ConnectorCapabilities` literal (fixes the `tsc` failure), the runtime `publish(evil, ...)` call wrapped in `expect(() => ...).toThrow()` (fixes the unguarded-throw failure), plus a second, real `// @ts-expect-error` block proving the everyday case — passing a Tier C connector to `publish()` directly, with no cast — fails to compile at all. `pnpm --filter @omnisell/connectors test` now passes (2/2 in this file, 52/52 package-wide) | none open | none — verify it stays green as the adapter set grows in later phases |
| 2-D1 | api | `S3PresignService` generates real, correctly-SigV4-signed presigned PUT URLs against `S3_ENDPOINT`/`S3_BUCKET` (presigning is pure local computation, needs credentials not connectivity) — but no MinIO/S3 is reachable in this sandbox, so an actual browser `PUT` against one of these URLs has never been exercised end-to-end here | `PRESIGNED` upload mode's endpoint (`POST /v1/assets/upload-init` with `mode: 'PRESIGNED'`) is real code, unverified round-trip | verify on a Docker-enabled machine with MinIO running; the web asset library UI defaults to `RESUMABLE` mode instead (2-D15) precisely because this can't be exercised here |
| 2-D2 | api | `ResumableUploadStorage` (backing the tus-like resumable upload session) writes chunks to local disk scratch storage (`ASSET_UPLOAD_SCRATCH_DIR`), not real S3 multipart upload — a deliberate, documented stand-in per the phase's own scoping allowance, since no live object storage exists here. The offset-tracking contract (`AssetUploadSession.receivedBytes`, strict-offset `appendChunk` rejection) is real and would not need to change when swapped for real S3 multipart | none — by design this phase; disk-backed | swap `appendChunk`/`readAll`/`cleanup`'s bodies for S3 multipart-upload SDK calls once live storage exists; the session/offset model above it stays the same |
| 2-D3 | api/web | The resumable-upload chunk endpoint (`PATCH /v1/assets/upload-sessions/:id`) accepts a JSON body (`{offsetBytes, chunkBase64}`) rather than the real tus wire protocol's raw `application/offset+octet-stream` PATCH body — a deliberate scope reduction (documented in `packages/shared/src/schemas/asset.ts`) so the endpoint works through Nest's default JSON body parser without extra raw-body middleware wiring. Offset-tracking semantics (a mismatched `offsetBytes` is rejected) match a real tus server; only the wire format differs | none — documented scope reduction | swap to a real tus server library (e.g. `@tus/server`) or raw-body middleware if full tus-client interoperability (not just this repo's own web/mobile clients) is ever required |
| 2-D4 | api | `MockupsService.compose()` calls `ObjectStorageService.getObject`/`putObject`, which need a live S3/MinIO connection (unlike presigning, these need real network access) — every call to `POST /v1/mockups/compose` will fail with a `503 object_storage_unreachable` in this sandbox, and the triggering `MockupRender` row is honestly recorded as `FAILED` with that reason (never a fabricated "READY" result). The underlying compositing logic (`MockupComposeService.compose` — resize/rotate/centre/composite via sharp) IS verified, against real sharp-generated bytes, in `apps/api/test/mockup-compose.service.test.ts` | `503 object_storage_unreachable` in this sandbox by design | verify the full HTTP path on a Docker-enabled machine with MinIO running and real scene/design objects uploaded |
| 2-D5 | api | Batch/async mockup rendering (featureslist.md 2.9 — "batch render") has no real worker: `MockupRenderQueue`'s only implementation, `NoopMockupRenderQueue`, honestly reports `{ queued: false }` rather than faking a queue that cannot actually run without Redis/BullMQ (unavailable this phase, same root cause as 0-D5). The synchronous single-mockup path (`MockupComposeService.compose`) is the only path this phase's controllers actually expose | `NoopMockupRenderQueue` | wire a real BullMQ queue once Phase 3's queue topology (task 3.6) lands |
| 2-D6 | api | `VirusScanService.scan()` is a logged no-op (`clean: true`, `engine: 'none (no scanner available in this sandbox)'`) — no ClamAV or equivalent daemon is reachable here. The call site is real (`AssetsService.completeResumableUpload` calls it and would reject the upload on `clean: false`), so wiring a real scanner later is a one-class change, not a new integration point | `engine: 'none (...)'` marker on every result — never presented as a real scan | integrate a real `clamd` client (or a cloud provider's S3-event virus-scan service) once available |
| 2-D7 | api | `ThumbnailService.extractMetadata` degrades to `{ widthPx: null, heightPx: null, dpi: null, hasTransparency: null, colorProfile: 'UNKNOWN' }` for any buffer sharp/libvips can't parse — confirmed in `apps/api/test/thumbnail.service.test.ts` with a garbage buffer. In practice this means SVG/PDF/AI/PSD assets (task 2.1's accepted file types) will often get null/UNKNOWN metadata rather than real values without extra system libraries (e.g. `librsvg`, `poppler`) installed alongside `sharp`/`libvips` — the preflight engine's rules already handle `null`/`UNKNOWN` inputs gracefully (WARN, not a crash), so this degrades safely, just less usefully for vector/document assets | rules report WARN/SKIPPED on missing metadata rather than crashing | install the relevant libvips companion libraries in the deployment image and re-verify vector/PDF metadata extraction on a real (non-sandboxed) machine |
| 2-D8 | api | `ProductsService`'s archive dependency guard (`assertNoLiveDependencies`, featureslist.md 3.12) is an intentionally EMPTY method — `Listing` (Publishing, Phase 4) doesn't exist in this schema yet, so there is nothing to guard against today. It is a real, named seam with the exact signature Phase 4 will need (`tenantId`, `productId`), not a fake check dressed up as real, and `ProductsService.archive` already calls it unconditionally before archiving so Phase 4 only has to fill in the body | none — documented forward-looking seam | Phase 4: once `Listing` exists, replace the body with `prisma.listing.count({ where: { tenantId, productId, status: { in: [...live statuses] } } })` and throw `ConflictException` when non-zero |
| 2-D9 | api | `FxRate` is a bare cache table with no ingestion job (featureslist.md 2.10's own "keep proportionate" instruction, full FX ingestion is Phase 6/Finance scope) — no seed rows were written this pass, and the pricing/margin engines don't automatically look up or apply an FX rate; `convertMinor` exists in `packages/shared/src/money-math.ts` as a pure, tested building block for a future caller, not wired into an automatic cross-currency price flow yet | none — deliberately minimal this phase | Phase 6: real FX rate ingestion (a scheduled job pulling from a rate provider) + wiring `convertMinor` into pricing-rule currency-floor conversion for cross-currency variant prices |
| 2-D10 | api | `ProductsService.importCsv`/`ProductsController.importCsv` accept a JSON `{ csv: string }` body (the web UI reads the `File` client-side via `file.text()` before posting) rather than a multipart file upload or a streamed parse — fine for the modest catalog sizes this phase targets, but the whole CSV is held in memory and each row does its own sequential DB round-trips (no batching), which would not scale to a very large catalog import | none — documented scale seam | move to multipart + streaming CSV parse + batched upserts if/when large-catalog import becomes a real requirement |
| 2-D11 | api | The margin preview engine's fee/tax model (`apps/api/src/catalog/margin/margin.engine.ts`) is a simplified flat-percentage-of-gross calculation (`channelFeePct`, `taxPct`) — not a real VAT/OSS-aware tax engine, which is explicitly Phase 6 (Finance) scope per `implentationplanphase.md`. The waterfall decomposition (gross → fees → print → shipping → tax → net) and margin % are real, correct arithmetic on whatever inputs are given; the inputs themselves aren't yet sourced from a real per-jurisdiction tax calculation | none — documented scope boundary | Phase 6: replace the flat `taxPct` input with the real Tax Centre's per-jurisdiction calculation once it exists |
| 2-D12 | mobile | `apps/mobile`'s new Studio (camera capture + auto-crop + resumable upload) and Catalog Products (read-only list/detail) screens are real code, exercised only via `vitest run` (esbuild, type-stripped) and manual code review — never on a physical device or emulator (no Docker, no device in this sandbox), the same standard Phase 1's biometric unlock held itself to. "Auto-crop" specifically means an automatic centre-square crop (`expo-image-manipulator`), not an ML subject-detection crop — there is no such model in scope this phase | untested-on-device; `1-D16` tracks the separate, unrelated `tsc` defect these screens also reproduce | run on a real device/simulator before shipping; add a Detox/Maestro E2E case for the capture → upload → list-refresh flow |
| 2-D13 | ui | Building `MarginWaterfallChart` (prompt.md "signature moment #1") surfaced a NEW manifestation of the dual-React-copy hazard already tracked in 1-D15/1-D16: `packages/ui` resolves its own pinned `react@19.0.0`, but `react-dom` (used by its `test/ui.test.tsx` via `react-dom/server`) resolves to the workspace root's `react@18.3.1` copy (pulled in by `apps/mobile`'s React Native 0.76 pin) — confirmed via `require.resolve` from `packages/ui`'s directory. Any hook-using component rendered through `renderToString` there throws `Invalid hook call` / `Cannot read properties of null (reading 'useState')`. Worked around for THIS component by writing it hook-free (a pure-CSS `scaleY` keyframe animation, gated by a plain `@media (prefers-reduced-motion: reduce)` override) — a genuinely better implementation for a chart whose only "state" was a mount-animation trigger, not a workaround-only fix. The underlying hazard remains open for any FUTURE hook-using component added to `packages/ui` | worked around via a hook-free component design for `MarginWaterfallChart`; no dependency-graph fix applied | same fix path as 1-D16: a root `pnpm.overrides` pin per React major version, scoped and tested for regressions before landing — do not retry a naive global override |
| 2-D14 | web | The Asset Library upload UI (`apps/web/lib/resumable-upload.ts`) always uses `RESUMABLE` mode, never `PRESIGNED` — the resumable path's chunks land on the API's own local disk scratch storage (2-D2), which is the only upload path that can actually complete end-to-end in this sandbox. `PRESIGNED` mode's endpoint is real (2-D1) and would work against a live S3-compatible endpoint in production; the web UI just has no way to prove that here | none — documented environment-driven default | switch the web UI to `PRESIGNED` mode (or offer both) once a live MinIO/S3 endpoint is available to verify against |
| 2-D15 | web | The new `/studio/assets`, `/catalog/products`, and `/catalog/products/[id]` pages were verified via `next dev` + `curl` (200 status for both `en` and `ar` locale cookies; `dir="rtl"` confirmed on `<html>` for other already-server-rendered pages) — the same ceiling Phase 1 hit. `next build` for `apps/web` is still blocked by the pre-existing 1-D15 defect (unrelated to this pass), and no real browser was available to visually verify drag-drop upload interactivity, the margin waterfall chart's rendered bars, or RTL mirroring (icon direction, sidebar alignment) on these specific new screens | none — same ceiling as 1-D15 | visual QA (drag-drop, chart rendering, RTL mirroring) on a real browser once `next build`'s React/Next version mismatch (1-D15) is resolved |
| 3-D1 | connector | `packages/connectors/src/adapters/*.ts` are real, live-doc-confirmed code (docs/CONNECTORS.md) but were NEVER exercised against a real authenticated Printful/Printify/Gelato/Prodigi account — this sandbox has outbound network access to fetch public documentation pages (confirmed, used for verification) but no real API credentials for any of the four, and per this task's own constraint, none were fabricated | `status: 'BETA'` (not `'ACTIVE'`) on all four registry rows; nightly `test/contract/*.contract.test.ts` skip cleanly without the relevant `*_CONTRACT_TEST_TOKEN`/`*_SANDBOX_API_KEY` env var | supply real credentials for a disposable/sandbox account per provider, run the nightly contract suite for real, then run api-registration.md §7's full checklist before flipping `status` to `'ACTIVE'` |
| 3-D2 | connector | Every endpoint URL in the four adapters was confirmed to exist via a live docs fetch, but several EXACT field/path shapes were not independently re-verified against a real authenticated call (flagged prominently in each adapter's own doc comment and in docs/CONNECTORS.md): Printful's OAuth token endpoint + `{code,result}` envelope; Printify's print-provider/variant sub-paths + `send_to_production.json`; Gelato's pricing-subdomain hostname (fetchCosts reads inline `price` instead) + orders API version; Prodigi's bulk-catalog-listing endpoint (doesn't exist per its own docs — `fetchBlueprints` uses a caller-supplied SKU seed list instead) + webhook payload shape | prominent code comments + docs/CONNECTORS.md per-provider "uncertainty flagged" sections | re-verify each flagged item against a real sandbox call before `'ACTIVE'` |
| 3-D3 | connector | `POST /v1/blueprints/sync` (real code, `BlueprintSyncService`, unit-tested against a fake adapter) cannot complete end-to-end in this sandbox — it needs an existing `CONNECTED` `Connection` with a real, working credential, and none exists here since no tenant has real provider credentials | 400/502 in practice when attempted against the seeded demo connection (which uses a fake credential value) | exercise for real once a tenant has a genuine Printful/Printify/Gelato/Prodigi credential |
| 3-D4 | queue | BullMQ queue topology (`ConnectorQueueService`, task 3.6) is real code — real `Queue`/`Worker` classes, real per-connector concurrency config, real DLQ/replay methods — but cannot move a job through Redis in this sandbox (no Docker, same root cause as 0-D2/0-D5). `startWorker()` exists and is callable but is never invoked during normal app bootstrap here (starting a real `Worker` would open a blocking connection that only retries forever against unreachable Redis) | ioredis connections use `lazyConnect: true` + a bounded `retryStrategy` (found necessary by actually booting the compiled app against no Redis — see the phase report's "Bugs found"); `/v1/readyz`'s `redis` check now does a real, time-boxed ping instead of a hardcoded `'degraded'` string (closes 0-D5) | verify `enqueue`/`replay`/`listFailed`/`startWorker` against a real Redis on a Docker-enabled machine |
| 3-D5 | connector | Token auto-refresh worker (`TokenRefreshService.runSweep`, task 3.13) is real, fully unit-tested logic (finds expiring credentials, decrypts, calls `adapter.refresh`, re-encrypts, writes back, alerts) — the only stubbed part is SCHEDULING: nothing currently calls `runSweep()` on a recurring basis. A real recurring trigger belongs on a BullMQ repeatable job (3-D4's same Redis gap) | `runSweep()` is real, callable, tested code; simply never invoked automatically | wire a BullMQ repeatable job calling `TokenRefreshService.runSweep()` once Redis is reachable |
| 3-D6 | connector | Printful is the only one of the four Phase 3 adapters offering OAuth2 in this phase. `PRINTFUL_OAUTH_CLIENT_ID`/`PRINTFUL_OAUTH_CLIENT_SECRET` are optional env vars, absent in this sandbox (same pattern as 1-D2's Google/Apple) — `buildAuthUrl`/`exchangeCode`/`refresh` are real, documented-endpoint code that will start working the moment real values are supplied, but the whole OAuth path is unreachable without them today | absent env vars mean `client_id=`/`client_secret=` are sent empty — Printful would reject the request, an honest failure, not a fake success | register a real Printful OAuth app and supply the credentials before enabling this path for real tenants |
| 3-D7 | connector | Per-connector rate limits recorded on the seeded `ConnectorDefinition.rateLimit` rows (120/60/60/60 req per 60s for Printful/Printify/Gelato/Prodigi respectively) are conservative ESTIMATES, not independently confirmed against each provider's own published numeric rate-limit documentation — none of the four docs pages fetched this pass stated an exact number in the excerpt retrieved | documented as an estimate in docs/CONNECTORS.md and docs/OPEN_QUESTIONS.md | confirm each provider's actual published rate limit and update the seed + `docs/CONNECTORS.md` |
| 3-D8 | web/admin | The new Channels pages (`/channels/connections`, `/channels/connections/[id]`, `/channels/capability-matrix`, `/channels/health`) and the admin `/connectors` registry screen were verified via `next dev` + `curl` (200 for `en`/`ar`, `dir="rtl"` confirmed) — the same ceiling every prior phase's web UI has hit here (no real browser, 1-D15/2-D15 unchanged and unrelated to this pass). Visual QA of the connection wizard's multi-step flow, the capability-matrix hover tooltip, and RTL mirroring of the wizard/table layouts is unverified beyond static HTML inspection | none — same ceiling as 1-D15/2-D15 | visual QA on a real browser once `next build`'s pre-existing React/Next mismatch (1-D15) is resolved |
| 3-D9 | api | Every Phase 3 Prisma model (`ConnectorVersion`, `TenantDataKey`, `Connection`, `Credential`, `ConnectorOAuthState`, `ConnectionHealthSample`) is schema-only — no migration has been generated/applied (same unrun-migration root cause as 1-D1/0-D2, now covering Phase 3's tables too). `infra/db/rls.sql` was extended with matching `tenant_isolation` policies for every one of them (verified: `prisma generate` succeeds against the schema, confirming it's syntactically valid) but is equally unapplied | `db:migrate`/`db:deploy` scripts exist but unrun | run `pnpm db:migrate` + `psql -f infra/db/rls.sql` on a Docker-enabled machine |
| 3-D10 | api | `Credential.encryptedSecondaryBlob` deliberately carries a different secret depending on `Credential.kind` (documented in `ConnectionsService.saveCredential`'s doc comment): for `OAUTH2` it's the refresh token; for `API_KEY` on Prodigi it's the other environment's (sandbox vs. live) key. This is an intentional design choice to avoid a near-duplicate column, but it does mean the column's meaning isn't self-evident from the schema alone — a future reader must know to check `kind` first | documented in the schema comment on `Credential` and in `ConnectionsService`/`AdapterRunnerService`'s doc comments | if this proves confusing in practice, split into two explicitly-named nullable columns instead |
| 4-D1 | api | The publish pipeline view's SSE stream (`createSyncJobStream`, `SyncJobsController`'s `@Sse(':id')`) is real, polling code — fully unit-tested with fake `fetchJob` timers (`test/sync-job-stream.test.ts`) — but extends the SAME gap 3-D4/1-D6 already tracked: nothing in this sandbox (no live queue worker) ever mutates a `SyncJob`/`SyncJobItem` row after the initial fan-out, so a live multi-second per-channel status progression cannot be demonstrated end-to-end here. The initial snapshot, the polling/diffing logic, and the terminal-status/max-duration completion are all real | `test/sync-job-stream.test.ts` proves the logic against fake state transitions; nothing fakes a live demo | wire a real BullMQ worker (3-D4) and re-verify the live SSE stream end-to-end on a Docker-enabled machine |
| 4-D2 | api | The publish orchestrator's Tier A/B path calls `ConnectorQueueService.enqueue(...)` for real — which cannot reach Redis in this sandbox (3-D4) and will throw. This is NOT swallowed or faked: `PublishOrchestratorService.processListingItem` catches the failure and records it as a real per-item `ERROR` (`SyncJobItem.status = 'FAILED'`, `Listing.status = 'ERROR'`, a `ListingEvent` row) — i.e. the Redis gap surfaces as an honest partial-success/failure, exactly the behaviour task 4.5 asks for, not a crash and not a fabricated success | real try/catch around every `queue.enqueue` call; `test/publish-orchestrator.service.test.ts`'s "captures a queue failure as a per-item error" test proves this path | re-verify with a reachable Redis; the orchestrator code does not need to change |
| 4-D3 | connector | `DriftDetectionService.check()` (task 4.13) is real, and its comparison logic (`drift.engine.ts`) is fully unit-tested — but it depends on `ConnectorAdapter.fetchListingState`, a NEW optional SDK method added this phase that NONE of the four real Phase 3 adapters (Printful/Printify/Gelato/Prodigi) implement: no live-doc-confirmed single-listing "get" endpoint was independently re-verified this pass (prompt.md constraint #2 forbids guessing one). `check()` therefore honestly returns `{ supported: false }` for every real connector today — `test/drift-detection.service.test.ts` proves the comparison logic works by injecting a fake adapter that DOES implement `fetchListingState`, not by faking the service's own honesty | `{ supported: false }` is the real, non-fabricated result for all four shipped adapters | re-verify each provider's single-listing "get" endpoint against a live account, implement `fetchListingState` per adapter, re-run `test/drift-detection.service.test.ts`'s fixtures against the real thing |
| 4-D4 | api | `ExportPackGeneratorService.generate()` (task 4.12's HTTP-facing half) calls `ObjectStorageService.getObject()` for the product's real primary-asset bytes — same live-MinIO dependency 2-D4 (Mockups) already tracked, unreachable in this sandbox. Any attempt to generate a pack for a listing tied to a real previously-uploaded asset will 503 here, same honest degrade as Mockups. The GENERATION logic itself (resize via `sharp`, ZIP assembly, metadata.csv, field-cards.html, real i18n CHECKLIST.md) is proven for real, independent of storage, in `test/export-pack-builder.test.ts` — synthetic image buffers, real `sharp` resize to the connector's confirmed spec, and a real round-trip through the system `unzip` binary. This is the ACTUAL proof this deliverable works, per the phase's own instruction to "actually generate the ZIP bytes in a test" | `test/export-pack-builder.test.ts` (4 tests, real ZIP bytes, real i18n, unzip-verified); `test/export-pack-generator.service.test.ts` proves the full generate→download→confirm flow with object storage mocked as reachable | verify the full HTTP path (`POST /export-packs`) on a Docker-enabled machine with MinIO running and a real uploaded design asset |
| 4-D5 | connector | Redbubble's registry row (`apps/api/prisma/connector-registry-seed.ts`) was promoted this phase from Phase 3's "boundary-proof only" `UNVERIFIED` row to a real, visible `status: 'BETA'` Tier C channel with a sourced `fieldSpec`. `maxTags: 15` and the `imageSpecs` entry are CONFIRMED via WebSearch against Redbubble's own July 2023 tagging-limits announcement and aggregated help-center guidance (help.redbubble.com itself 403'd this pass's `WebFetch`, so the citation trail goes through secondary sources — see `docs/CONNECTORS.md`). `maxTitle`/`maxDescription` are conservative ESTIMATES — no official character-cap source was found, same honest-estimate pattern as 3-D7's rate limits | code comments on the Redbubble seed row + `docs/CONNECTORS.md` mark exactly which numbers are confirmed vs. estimated | confirm the exact title/description caps against Redbubble's current help-center pages once they're reachable via `WebFetch`, or a live account |
| 4-D6 | api | `PublishInputBuilderService.build()` can only resolve a real `externalBlueprintId`/`providerVariantId` when the product's cached `Blueprint.providerSlug` matches the TARGET channel's connector slug — there is no cross-provider blueprint mapping anywhere in this codebase (Phase 2's `Blueprint` cache is per-provider by design, docs/OPEN_QUESTIONS.md #19). Publishing a product whose only cached blueprint is from Printful to a Printify connection will produce an empty `externalBlueprintId` and a real warning surfaced in the dry-run/publish response, rather than a fabricated ID — `test/publish-input-builder.service.test.ts` proves both the matching and non-matching cases | real warnings array on every `BuiltPublishInput`, surfaced in the dry-run UI | build a real cross-provider blueprint/variant mapping table if multi-provider publishing from one cached blueprint becomes a real requirement |
| 4-D7 | api | Bulk actions' `REPRICE`/`RETAG` (task 4.8) are LOCAL-ONLY this phase — they update OmniSell's own `Listing`/`ListingVariant` rows but do NOT call `adapter.update()` to push the new price/tags to an already-LIVE channel. This is a deliberate scope reduction (same class as 2-D2/2-D3's documented scope reductions), not a fake success: the bulk-action response is honest about what actually changed (OmniSell's own records), and `RESYNC`/drift detection exists precisely to surface the resulting local-vs-channel divergence afterwards. Only `PUBLISH`/`UNPUBLISH` touch a real adapter call in this phase's bulk actions | `BulkActionsService`'s doc comment states this explicitly; `test/bulk-actions.service.test.ts` tests the real local-only behaviour, not a live push | wire `REPRICE`/`RETAG` to also enqueue a real `update`-kind queue job (reusing `PublishInputBuilderService`) once this is a real product requirement |
| 4-D8 | api | Bulk `RETAG` has no automatic undo (unlike `REPRICE`, which returns a real, restorable `{listingVariantId, priceMinor, currency}` token per item and a working `POST /listings:bulk-undo-reprice`) — reversible only by a human manually re-running `RETAG` with the previous tag list. This is an honest gap, not a fake "undo" button: task 4.8 explicitly asks to "be honest about which bulk actions are truly reversible and which aren't" | none — documented limitation | add a `ListingFieldOverride`-style history table if automatic retag-undo becomes a real requirement |
| 4-D9 | api | Bulk `UNPUBLISH` genuinely refuses (real `{ ok: false, error }`, no adapter call attempted) for any connector whose `capabilities.canAutomate !== true` or `capabilities.canUnpublish !== true` — i.e. every Tier C channel and Prodigi. This is the intended, honest behaviour (brb.md §6: never automate what a channel doesn't expose), not a bug: `test/bulk-actions.service.test.ts`'s "honestly refuses for a Tier C connector" test is the proof | real capability check before ever reaching `AdapterRunnerService.run` | none open — this is by design |
| 4-D10 | api | Every Phase 4 Prisma model (`Listing`, `ListingVariant`, `ListingFieldOverride`, `ListingEvent`, `SyncJob`, `SyncJobItem`, `ExportPack`, `ExportPackItem`, `BannedTerm`) is schema-only — no migration generated/applied (same unrun-migration root cause as 1-D1/0-D2/3-D9). `infra/db/rls.sql` was extended with matching `tenant_isolation` policies for every tenant-scoped one of them (`BannedTerm` deliberately excluded — it's global, same reasoning as `ConnectorDefinition`) — verified via `prisma generate` succeeding against the schema, but equally unapplied. The new `seedPublishing`/`seedBannedTerms` seed functions are real, defensive (early-return if their dependencies are missing) code, never executed against a live DB here | `db:migrate`/`db:deploy` scripts exist but unrun | run `pnpm db:migrate` + `psql -f infra/db/rls.sql` + `pnpm db:seed` on a Docker-enabled machine |
| 4-D11 | web/admin | The new web pages (`/listings/*`, `/channels/sync-queue*`, `/channels/export-packs`) and admin pages (`/moderation`, `/jobs`) were verified via `next dev` + `curl` (200 for `en`/`ar`, `dir="rtl"` confirmed) — the same ceiling every prior phase's web UI has hit (1-D15/2-D15/3-D8, unrelated to this pass). The composer's live character/tag counters, the publish pipeline view's SSE-driven card updates (signature moment #2), and RTL mirroring of the new tables/forms are unverified beyond static HTML + `dir="rtl"` inspection — no real browser exists in this sandbox | none — same ceiling as 1-D15/2-D15/3-D8 | visual QA once `next build`'s pre-existing React/Next mismatch (1-D15) is resolved |
| 4-D12 | mobile | The new mobile screens (`app/(tabs)/listings.tsx`, `app/listings/[id].tsx`, task 4.14) reproduce the exact same pre-existing 1-D16 `tsc` defect class every other mobile screen already hits (confirmed: the error text/pattern is identical, and `apps/mobile#typecheck` was already failing before this phase for unrelated files) — not a new defect. Verified via `vitest run` (esbuild, type-stripped) and code review only, never on a real device/emulator, same standard Phase 1/2's mobile work held itself to | 1-D16 (unchanged, pre-existing) | run on a real device/simulator before shipping; fix 1-D16's root cause (duplicate `@types/react` in the pnpm graph) separately |
| 4-D13 | api | `OmniSellClient.streamSse`/`downloadBlob` (`packages/api-client/src/client.ts`) deliberately do NOT use the browser's native `EventSource`/`<a href>` download — `EventSource` cannot attach an `Authorization` header and this app's auth is a Bearer JWT, not a cookie session (a real, structural mismatch, not an oversight). Both are real `fetch`-based implementations (manual SSE frame parsing / blob download) verified in `packages/api-client/test/client.test.ts` (11 tests) — genuinely functional in any modern browser, just not through the API a native `EventSource` exposes | none — this is the real, working solution, not a stub | none open |
| 4-D14 | Tooling | `prisma generate` and `pnpm install` both hit a transient Windows `EPERM`/`ENOENT` race in this sandbox multiple times this pass (renaming the compiled query-engine `.dll.node`, extracting a freshly-fetched package into `node_modules`) — always resolved on a bare retry (occasionally needed clearing `node_modules/.prisma/client` first). Root cause looks like antivirus/filesystem-indexer lock contention on Windows, not an OmniSell code defect — same class of environment flakiness as this repo's other Windows-specific sandbox quirks (docs/DEBT.md 0-D1) | none — retry resolves it every time observed | if this recurs often enough to matter, retry the exact command with a short backoff in CI on Windows runners |

<!-- Phase 4 debt entries end here. Phase 4.5 (Points Economy) debt entries follow; see docs/OPEN_QUESTIONS.md for genuine ambiguities resolved this pass. -->

| 4.5-D1 | api | The server-side video-duration probe (docs/points-extension.md §9.4) is REAL, not a stand-in — this sandbox has a genuinely working `ffprobe`/`ffmpeg` on PATH, confirmed by round-tripping a real `ffmpeg`-synthesized MP4 through `VideoProbeService.probeFile` and asserting the reported duration matches what was actually encoded (`apps/api/test/video-probe.service.test.ts`), and separately by downloading a real public sample MP4 over the network and probing ITS true duration too (verified manually in this pass; the automated test only covers the locally-synthesized case to avoid a flaky network dependency in CI). `VideoContentService.create()` supports two sources: `uploadSessionId` (bytes already on this API's own resumable-upload scratch storage, reusing Phase 2's pipeline unmodified) or an external `url`, which the server downloads (bounded to 200MB, 15s timeout) and probes — never trusting a client-supplied duration in either path. **The one real, undone hardening gap**: fetching an arbitrary caller-supplied `url` server-side has no SSRF protection (no private-IP/localhost/link-local block, no domain allow-list) | none — documented in `VideoContentService`'s own doc comment | add an SSRF guard (block RFC 1918/loopback/link-local resolution, optionally a domain allow-list) before accepting untrusted admin input in a real multi-tenant deployment |
| 4.5-D2 | api | `PointsQueueService` (real BullMQ `Queue`/`Worker`/DLQ, same topology pattern as Phase 3's `ConnectorQueueService`) cannot move a job through Redis in this sandbox (no Docker, same root cause as 0-D2/0-D5/3-D4). `VideoWatchService.complete()` calls `enqueueValidation()` first; on failure (the expected, observed outcome here) it falls back to running the EXACT SAME validation logic (`awardIfEligible` — cap/cooldown enforcement + the new `PointTransaction` + wallet crediting, all in one DB transaction) synchronously in the same request, rather than leaving the user's watch un-awarded. This is a deliberate design choice (documented in the class's own doc comment), not a silent stub: the moment a real Redis exists, the enqueue succeeds and a dedicated worker process calling `PointsQueueService.startWorker(...)` moves validation off the request path with zero changes to `VideoWatchService` | real try/catch + documented inline fallback; `apps/api/test/video-watch.service.test.ts` exercises the fallback path (the only path reachable in this sandbox) | verify the enqueue path against a real Redis, then start a dedicated worker process calling `awardIfEligible` as its processor |
| 4.5-D3 | api | `ExpiryService.runExpirySweep`/`sendExpiryReminders` (§7.5, task 4.5.6 — "P2 backend-ready") are real, callable, DB-transactional logic — same class as 3-D5's `TokenRefreshService.runSweep` — but nothing calls them on a recurring schedule; there is no BullMQ repeatable job (no Redis, 0-D5) and no `@nestjs/schedule` cron wiring in this pass. Correct as long as something calls them; today nothing does automatically | real, unscheduled methods; no admin endpoint exposes a manual trigger yet either | wire a BullMQ repeatable job (or `@nestjs/schedule` cron, or a manual admin trigger endpoint) once Redis is reachable or the product wants a manual "run now" button |
| 4.5-D4 | mobile | The new consumer screens this phase touches (`app/consumer/{wallet,videos,shop}.tsx` rewritten for real; `app/consumer/video/[id].tsx` new) are real, working code wired to the real Phase 4.5 API — parse-checked clean via ESLint's TypeScript parser (0 errors) — but never rendered or exercised on a real device/emulator/simulator, the same standing limitation every prior mobile pass documented (1-D4/2-D12/4-D12). `apps/mobile`'s pre-existing `tsc` defect (1-D16, duplicate `@types/react` in the pnpm graph) means these files cannot be fully type-checked here either — the same ceiling, not a new one. `expo-av` (video playback) and `expo-crypto` (idempotency-key UUIDs) were added as new dependencies this pass specifically for this work and are equally unverified on-device | 1-D16 (unchanged); ESLint parse-check is the real gate actually run here | run on a real device/simulator before shipping; add a Detox/Maestro E2E case for the full watch→earn→wallet-refresh loop |
| 4.5-D5 | web | The new web pages (`/consumer/{wallet,videos,shop}`, `/points/{rules,videos,fraud-queue,adjust}`) and the sidebar's new Creator⇄Consumer mode-switch button were verified via `next dev` + `curl` (200 for `en`/`ar`, `dir="rtl"` confirmed on the Arabic-cookie response) — the same ceiling every prior phase's web UI has hit (1-D15/2-D15/3-D8/4-D11, unrelated to this pass; `apps/web`'s `tsc --noEmit` is independently broken by the SAME pre-existing dual-React-copy defect class as 1-D15, confirmed by the identical `'X' cannot be used as a JSX component` error pattern appearing across dozens of already-existing, untouched files, not just the ones this pass added). The consumer video player's native `<video controls>` interactivity, the shop page's points range-input slider, and the `aria-live` earned-points announcement are unverified beyond static HTML inspection — no real browser exists in this sandbox | none — same ceiling as 1-D15/2-D15/3-D8/4-D11 | visual QA (video controls, slider, aria-live announcement, RTL mirroring) once `next build`'s pre-existing React/Next mismatch (1-D15) is resolved |
| 4.5-D6 | api | **CLOSED this pass (Phase 5).** `Order`/`OrderItem` now exist; `RedemptionService.computeMath` accepts an optional `orderId` and, when supplied, uses the real `Order.subtotalMinor`/`Order.currency` instead of the single product's `priceMinor` — the floor/share-cap/rate math itself is byte-for-byte unchanged, only what "subtotal" resolves to. `ProductPurchaseWithPoints.orderId` is a real FK to `Order` now (was a bare unlinked `String?`). Also closed: the order-cancellation/refund wiring — `OrdersService.transition` calls `ProductPurchaseWithPointsRepository.findConfirmedByOrderId` + `RedemptionService.refund` for every confirmed redemption on an order that becomes `CANCELLED`/`REFUNDED`, proven in `apps/api/test/orders.service.test.ts` (3 tests, mocked repos) — this actually FIRES from the real transition code path, not merely available in isolation. The single-product path (`orderId` omitted) stays exactly as it was, for previewing/confirming a redemption before an Order row exists | real code, real tests | none open for this specific gap — see 5-D3 below for the (separate, new) caveat on cost attribution for PARTIAL refunds |
| 4.5-D7 | Tooling | During this pass, `apps/api`'s generated Prisma client (`node_modules/@prisma/client`, the default un-namespaced output location) was observed to intermittently contain the WRONG schema's types (a much smaller model set with no `Tenant`/`Membership`/`AuditLog` — matching neither this app's schema nor, on inspection, `apps/marketplace-api`'s schema, which deliberately uses a custom `output` path specifically to avoid this exact collision per its own schema comment) and to hit the same `EPERM: ... query_engine-windows.dll.node` race documented in 4-D14 far more persistently than a simple antivirus-lock explanation accounts for. The most likely explanation is a concurrent process elsewhere in this monorepo also running `prisma generate` against the same shared default output directory at the same time this pass was running. Retrying `prisma generate` immediately before every typecheck/test run resolved it every time observed in this pass (confirmed stable across 5 consecutive retries once the interference stopped) | retry-before-verify; no permanent fix applied this pass | give `apps/api` its own custom generator `output` path (the same pattern `apps/marketplace-api` already uses) to eliminate the shared-resource collision entirely — a larger, deliberately-deferred change since it touches every repository file's `@prisma/client` import |
| 4.5-D8 | api | `@nestjs/throttler` (named in docs/points-extension.md §4 as the intended rate-limiting library) is not installed in this workspace. Built instead: a real, DB-authoritative per-user daily watch-session count check (`VideoWatchService.start` counting today's `VideoWatch` rows against `POINTS_MAX_WATCHES_PER_DAY`) — genuinely enforced, not stubbed. NOT built: the separate per-IP throttle on `start`/`heartbeat` §8.3 also calls for (`points-watch-ip` keying) — no IP-based rate limiter exists in this pass | per-user daily cap is real and tested; per-IP throttling is a genuine, undone gap | add an IP-keyed limiter (in-memory sliding window is enough without Redis; a real token-bucket via `@nestjs/throttler` once Redis is reachable) |
| 4.5-D9 | api | Realtime wallet balance (§4 — "SSE (web); polling fallback (mobile, 30s)") is NOT implemented this pass — `GET /v1/wallet` is poll-based only on both web and mobile, matching the exact same documented precedent as Phase 1's notification centre (1-D6: "poll-based, not push-based"). No SSE endpoint exists for the wallet | none — poll-only by omission, not by a fabricated push mechanism | wire an SSE stream (reusing the publish-pipeline view's `streamSse` pattern from `packages/api-client`) once a live-balance-changed event source exists to stream from |
| 4.5-D10 | api | §8.2 calls for device fingerprints to be "hashed non-personally (SHA-256 of the components); raw identifiers are never persisted." `VideoWatch.deviceFingerprint` in this pass stores exactly whatever string the client sends (`startVideoWatchSchema`'s `deviceFingerprint` field), unhashed — a real, honest gap, not a silently-broken hashing step. The column and its use in the IP/device fan-out fraud signal (`FraudService.detectIpDeviceFanout`) are real; only the "hash before persisting" step is missing | none — client-supplied value stored as-is | hash (SHA-256) the fingerprint components server-side before persisting, on both the web canvas-fingerprint path and the mobile install-scoped-`deviceId` path, before this ever reaches a real user's device identifier |
| 4.5-D11 | api | Every new Phase 4.5 Prisma model change (VideoWatch's `lastHeartbeatAt`/`lastWatchPosition`/`fraudSignals` columns, `PointTransaction`'s new `@@unique([tenantId, source, sourceId])` constraint, and the new `LedgerEntry`/`LedgerLine` tables) is schema-only — no migration generated/applied, same unrun-migration root cause as every prior phase (1-D1/3-D9/4-D10). `infra/db/rls.sql` was extended with matching `tenant_isolation` policies for `LedgerEntry`/`LedgerLine` and the previously-missing `TenantPointSettings` policy was added too (a real gap in the Phase 0 scaffold, closed this pass) — verified via `prisma generate` succeeding against the schema, but equally unapplied | `db:migrate`/`db:deploy` scripts exist but unrun | run `pnpm db:migrate` + `psql -f infra/db/rls.sql` on a Docker-enabled machine |
| 4.5-D12 | web/mobile | The Creator⇄Consumer mode switcher (§10.1) persists client-side only — `localStorage` on web (keyed by user id, same pattern the sidebar's own collapse state already uses) and the pre-existing "More" menu navigation on mobile (no new persisted toggle added there this pass; mobile already reached the consumer screens via `MORE_SECTIONS`'s `nav.consumerMode` group per the Phase 0 scaffold, which this pass left as the mobile mode-entry point rather than rearchitecting the bottom-tab layout). Neither syncs across devices — a real, deliberate scope choice given the time budget, not a broken sync | none — documented client-side-only persistence | add a server-side user preference field if cross-device mode-memory becomes a real requirement |

<!-- Phase 4.5 debt entries end here; see docs/OPEN_QUESTIONS.md for genuine ambiguities resolved this pass. -->

| 5-D1 | api | Every new Phase 5 Prisma model (`Order`, `OrderItem`, `OrderWebhookEvent`, `OrderPollCursor`, `OrderFee`, `OrderException`, `OrderEvent`, `FulfilmentRoutingRule`, `Fulfilment`, `Shipment`, `TrackingEvent`, `Return`, `Refund`, `Reprint`, `SavedOrderView`, `BuyerMessageTemplate`, `BuyerMessageLog`, `DigitalProduct`, `DigitalFile`, `DigitalFileVersion`, `Entitlement`, `DeliveryToken`, `DeliveryLog`, `LicenceKey`, `LicenceKeyActivation`, `Coupon`, `CouponRedemption`) is schema-only — no migration generated/applied, same unrun-migration root cause as every prior phase (1-D1/3-D9/4-D10/4.5-D11). `infra/db/rls.sql` was extended with matching `tenant_isolation` policies for all 27 tenant-scoped tables above — verified via `prisma generate` succeeding against the schema (confirmed clean, `apps/api`'s `tsc --noEmit` passes with 0 errors against the extended schema), but equally unapplied | `db:migrate`/`db:deploy` scripts exist but unrun | run `pnpm db:migrate` + `psql -f infra/db/rls.sql` on a Docker-enabled machine |
| 5-D2 | api | Order ingestion (`OrderIngestionService`, task 5.1) is real, working code for both webhook and poll paths, sharing one `upsertNormalisedOrder` code path by design (see the class's own doc comment) — but has never moved a real webhook delivery or `pullOrders` page through this sandbox: no real connector credentials exist (same root cause as 3-D1/3-D3), and the webhook receiver itself receives Nest's ALREADY-JSON-PARSED body rather than raw bytes (`OrderWebhooksController`'s doc comment), so `JSON.stringify(body)` stands in for "the raw request" when computing the HMAC signature — real HMAC code (`order-webhook-verification.util.ts`, 7 passing unit tests against synthetic secrets/bodies), but not guaranteed to validate a genuinely correctly-signed real provider request, since key ordering/whitespace can differ from what the provider originally signed. Also: no dedicated `webhookSecret` field exists on `Connection`/`Credential` yet — the connection's primary API credential stands in as the HMAC key, a real, documented placeholder (see `OrderIngestionService`'s doc comment) | `order-webhook-verification.util.test.ts` proves the HMAC logic itself is correct against a byte-identical body; the JSON-reserialization gap and missing-webhook-secret-field gap are both real, undone | add a raw-body-capturing middleware ahead of the JSON parser for `POST /hooks/:slug/:connectionId` specifically, and add a dedicated `Connection.webhookSecret` (or a new `Credential.kind`) once a real webhook subscription flow is built per-provider |
| 5-D3 | api | Auto-routing (`FulfilmentService`/`fulfilment-routing.engine.ts`, task 5.4) builds its candidate list from real `Connection`/`ConnectorDefinition` rows (real connectorSlug, real `canFulfil` capability check) but WITHOUT live per-candidate cost/ETA data — no live-authenticated `fetchCosts`/carrier-rate call exists end-to-end for a specific destination+SKU this phase. `CHEAPEST`/`FASTEST` therefore only pick a winner when cost/ETA data happens to be supplied (proven with synthetic data in `fulfilment-routing.engine.test.ts`, 9 tests); against this phase's real candidate-building step they degrade to "no rule matched, manual choice required" rather than a fabricated winner. The routing ENGINE itself is fully real and correct — this is a real-world-data-enrichment gap, documented in `FulfilmentService`'s own doc comment | honest `ConflictException` ("no routing match") rather than a guessed provider | wire real per-candidate cost/ETA (Phase 3's `fetchCosts` per connection + a carrier-rate source) once needed |
| 5-D4 | api | Packing-slip/commercial-invoice PDF generation (task 5.8) uses `pdf-lib` (confirmed working in this sandbox — a real PDF was generated and byte-inspected, `%PDF-` magic header, during this pass; no native bindings, pure JS). The bilingual DATA (real English AND Arabic label strings, `PackingSlipService`'s `BILINGUAL_LABELS`) is real and complete for both locales. The GAP: `pdf-lib`'s bundled `StandardFonts` are WinAnsi/Latin-only and cannot render Arabic glyphs — no Arabic-script font (e.g. Amiri, Noto Naskh Arabic) is bundled in this sandbox to embed via `fontkit`. The EN-locale PDF renders correctly; the AR-locale PDF's Arabic label text will render as missing-glyph boxes until a real Arabic font is embedded | documented in `PackingSlipService`'s own doc comment — never claimed to render Arabic glyphs correctly | embed a real Arabic-supporting TTF via `@pdf-lib/fontkit` once one is available in the deployment image, then re-verify the AR PDF visually |
| 5-D5 | api | Digital delivery's signed URLs (`DeliveryService`, task 5.10/7.2) reuse Phase 2's `S3PresignService` (extended with a new `presignGet` method) for the actual SigV4 signing — the TTL/download-count/IP-cap enforcement logic itself is REAL and unit-tested (`delivery.service.test.ts`, 6 tests: expired, cap-reached, IP-mismatch, revoked-entitlement, and the happy path all proven against a mocked repository layer). Same honest gap as every other S3-touching feature since Phase 2 (2-D1/2-D4): no live MinIO/S3 endpoint is reachable in this sandbox, so the actual `presignGet` round-trip (a real browser/buyer `GET` against the signed URL) has never been exercised end-to-end here | real, unit-tested cap/TTL/IP logic; unverified live S3 round-trip (same class as 2-D1/2-D4) | verify on a Docker-enabled machine with MinIO running and a real uploaded digital file |
| 5-D6 | mobile | The offline order queue (`apps/mobile/lib/offline-queue.ts`, task 5.13 — "offline cache + queued mutations, conflict-safe sync on reconnect") is a genuinely NEW capability this phase (no prior mobile pass built one) — real, dependency-injected logic proven with 6 real `vitest` tests (enqueue, successful flush, network-failure retry-left-queued, 409-conflict moved to a separate conflicts list rather than retried forever, conflict discard, FIFO ordering). Wired into real screens (`app/(tabs)/orders.tsx`, `app/orders/[id].tsx`) via a new `@react-native-async-storage/async-storage` dependency (added this pass) for the real persisted storage backing it. Same standing limitation as every prior mobile pass (1-D4/2-D12/4-D12/4.5-D4): never exercised on a real device/emulator/simulator (no Docker, no device in this sandbox) — `apps/mobile`'s pre-existing `tsc` defect (1-D16) means these files also can't be fully type-checked here, the same ceiling, not a new one; `eslint`/`vitest` (both clean, 0 errors / 9 passing) are the real gates exercised | 1-D16 (unchanged); `vitest`/`eslint` are the real gates run here | run on a real device/simulator before shipping; add a Detox/Maestro E2E case for the queue-while-offline → reconnect → sync flow; add `@react-native-community/netinfo` for PROACTIVE connectivity detection instead of today's REACTIVE "catch a network error, then queue" approach if a product requirement needs it |
| 5-D7 | web/admin | The new web pages (`/orders`, `/orders/[id]`, `/orders/exceptions`, `/digital/files`, `/digital/coupons`) and the new admin page (`/order-exceptions`) were verified via `next dev` + `curl` (200 for `en`/`ar` locale cookies; `dir="rtl"` confirmed on the Arabic response for both apps) — the same ceiling every prior phase's web UI has hit (1-D15/2-D15/3-D8/4-D11/4.5-D5). `next build`/`tsc --noEmit` remain blocked by the pre-existing dual-React-copy defect (1-D15), unrelated to this pass. Visual QA of the order-detail action buttons (prompt-based hold/cancel/refund/reprint inputs — a deliberate, minimal `window.prompt` UI rather than a full modal form, given this phase's scope), the exceptions table's SLA-breach highlighting, and RTL mirroring of all six new pages is unverified beyond static HTML + `dir="rtl"` inspection — no real browser exists in this sandbox | none — same ceiling as every prior phase | visual QA once `next build`'s pre-existing React/Next mismatch (1-D15) is resolved; consider replacing the `window.prompt` action inputs with real modal forms if this UI graduates past this phase's minimal-scope pass |
| 5-D8 | web | Sidebar sub-navigation for Orders/Digital Products is only PARTIALLY replaced this pass: `/orders`, `/orders/exceptions`, `/digital/files`, and `/digital/coupons` are real pages; `/orders/unfulfilled`, `/orders/in-production`, `/orders/shipped`, `/orders/returns`, `/digital/licences`, `/digital/delivery-log`, and `/digital/entitlements` still fall through to the `(shell)/[...slug]` coming-soon catch-all (the real backend endpoints for all of these exist and are exercised by API-level tests — this is a web-UI-surface scope reduction only, given this phase's time budget, not a missing backend). The main `/orders` feed's `status`/`search` filters can reach the same data these sub-views would show | `(shell)/[...slug]` catch-all for the un-built sub-views | build the remaining status-filtered sub-views (each is a thin wrapper around `OrdersListView` with a fixed status/view prop, mirroring `ListingsListView`'s own `published`/`pending`/etc. pattern) once prioritised |
| 5-D9 | api | `OrderExceptionService.runSlaBreachSweep` and `OrderIngestionService.runPollSweep` (tasks 5.1/5.6) are real, callable, DB-transactional logic — same class of gap as `TokenRefreshService.runSweep`/`ExpiryService.runExpirySweep` (3-D5/4.5-D3) — nothing calls either on a recurring schedule (no reachable Redis for a BullMQ repeatable job, no `@nestjs/schedule` cron wiring this pass) | real, unscheduled methods; no admin "run now" endpoint exposes them yet either | wire a BullMQ repeatable job (or `@nestjs/schedule` cron, or a manual admin trigger endpoint) once Redis is reachable or the product wants a manual trigger |
| 5-D10 | api | Partial refunds (`ReturnsRefundsService.issueRefund`) do NOT trigger the points-redemption-refund wiring — only a refund that reaches or exceeds the order's `totalMinor` (i.e. becomes a full refund, transitioning the order to `REFUNDED`) does, via `OrdersService.transition`'s real wiring (see 4.5-D6, closed). A genuine open question: should a PARTIAL refund proportionally restore SOME points too? Conservative default implemented: no — points are only restored on a full refund or an outright cancellation, never partially, since the points-math (floor/share-cap) would need a proportional-restoration rule this phase's spec doesn't define | documented here and in docs/OPEN_QUESTIONS.md | revisit if product wants proportional points restoration on partial refunds |
| 5-D11 | api | Actually executing a refund against a real payment processor or connector's own refund API (e.g. Stripe, or a marketplace's order-refund endpoint) is NOT implemented — `Refund` rows are real internal bookkeeping (real cost-attribution breakdown, real idempotency, real status machine) marked `COMPLETED` immediately since no payment-gateway integration exists in this codebase yet (documented in `ReturnsRefundsService`'s own doc comment). This is explicitly Phase 6 (Finance) scope, not a bug in this pass | documented in `ReturnsRefundsService`'s doc comment | wire a real payment-gateway refund call once Phase 6's billing/payments integration exists |

<!-- 5-D* entries end here. A bounded follow-up pass (not a numbered phase) then added six more connector adapters; see docs/CONNECTORS.md's "Six more adapters" section for the full per-provider citation trail. -->

| F1-D1 | connector | Six more adapters landed in a bounded follow-up pass — Etsy, Shopify, WooCommerce, Gumroad, Payhip, Sellfy (`packages/connectors/src/adapters/{etsy,shopify,woocommerce,gumroad,payhip,sellfy}.ts`), each verified live via `WebFetch`/`WebSearch` against its own real developer-docs domain (docs/CONNECTORS.md has the full per-provider citation trail) and each with real MSW-mocked unit tests (91 passing package-wide this pass, up from 52 before it — `packages/connectors/test/{etsy,shopify,woocommerce,gumroad,payhip,sellfy}.adapter.test.ts`). **Precisely what "landed" means here, mirroring 3-D1/3-D2's own honesty standard**: real code, real types, MSW-tested — NOT exercised against any real authenticated account for any of the six (no credentials, no live network access to a real tenant's Etsy/Shopify/WooCommerce/Gumroad/Payhip/Sellfy account exists in this sandbox — same distinction 3-D1 already draws between fetching public docs pages, which this pass did, and making an authenticated API call, which it did not). Two of the six required an honest step beyond "unexercised": Payhip and Sellfy are seeded with a deliberately REDUCED capability set (no `publish`/`update`/`unpublish`/`pullOrders` methods at all) rather than the generic full-automation block the other eight adapters use, because live verification this pass found their public APIs genuinely do not expose product/listing or order endpoints — api-registration.md §2.3 itself flagged both as uncertain, and this pass confirmed rather than assumed the gap. Sellfy goes further still: its `verifyCredentials` calls a real, confirmed, but unauthenticated endpoint (oEmbed) and says so loudly in its own doc comment, rather than presenting that call as equivalent to the credential checks the other nine adapters perform | `status: 'BETA'` (not `'ACTIVE'`) on all six new registry rows, the same as the original four; nightly contract-test scaffolding (3-D1's pattern) was not extended to these six this pass — see the "Planned fix" column | supply real credentials for a disposable/sandbox account per provider, run api-registration.md §7's full checklist before flipping any of the six to `'ACTIVE'`, and add nightly `test/contract/*.contract.test.ts` files for them mirroring the original four's pattern |
| F1-D2 | connector | Several endpoint shapes across the six new adapters were confirmed to exist by name/path via a live docs fetch or WebSearch this pass but were not independently re-verified against a real authenticated call — flagged prominently in each adapter's own doc comment and in docs/CONNECTORS.md's per-provider sections: Etsy's exact listing-creation/update field envelope and the tracking-submission endpoint; Shopify's GraphQL mutation field-selection sets (mutation/query NAMES are confirmed, literal selections are not); WooCommerce's webhook registration call and the `?force=true` delete convention; Gumroad's OAuth host pair, response envelope shape, and the `mark_as_shipped`/product update-delete paths; Payhip's and Sellfy's gaps are the deeper "no endpoint exists at all" kind already covered by F1-D1, not this narrower "endpoint exists, exact shape unconfirmed" kind | prominent code comments + docs/CONNECTORS.md per-provider "uncertainty flagged" sections | re-verify each flagged item against a real sandbox call before `'ACTIVE'` |
