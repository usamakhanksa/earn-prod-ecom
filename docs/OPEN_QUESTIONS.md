# OmniSell — Open Questions & Conservative Defaults
> Rule (prompt.md): when something is ambiguous, implement the most conservative
> interpretation, record the default here, and keep building.

| # | Question | Conservative default implemented | Revisit when |
|---|---|---|---|
| 1 | Package consumption: build `dist` vs consume TS sources | Packages compile to `dist` (CJS) via `tsc`; apps consume built output. Turbo orders `^build` first. | If HMR DX degrades, switch source consumption with `tsx`/bundler resolution |
| 2 | ESM vs CJS for packages | Packages + API compile to CommonJS (Node 10 resolution, plain `require`); `config` tooling is ESM. | When the API moves to an ESM Nest build |
| 3 | `pointsPerCurrencyMinor` direction | Points *per one minor unit* of discount; integer-friendly floor/floor math per `docs/points-extension.md` §6.2 | Product decision on rate granularity |
| 4 | `/v1/wallet/earn/video-watch` session semantics | Thin alias over the canonical start/heartbeat/complete pipeline; unverified `watchSeconds` is rejected | Implemented in Phase 4.5 |
| 5 | Global daily cap | `POINTS_DAILY_EARNING_CAP=500`; per-action caps stack under it | Phase 4.5 tuning |
| 6 | Health semantics | `/v1/healthz` = liveness (always 200); `/v1/readyz` = per-dependency status in a 200 body (never 503) so partial outages degrade gracefully | Add 503 semantics + SLO if ops prefer gateway-style readiness |
| 7 | Enum storage | Free strings validated by zod; DB CHECK constraints deferred | Phase 1 migrations |
| 8 | `cuid()` vs `cuid2()` | Prisma `@default(cuid())`; `cuid2` column default not available out of the box | Schema upgrade migration |
| 9 | Mobile locale | **Resolved in the Phase 1 follow-up pass**: `apps/mobile/lib/locale.ts` persists a user-chosen locale via `expo-secure-store` (`/more/language` screen), defaulting to `en`. React Native's RTL mirroring (`I18nManager`) only fully applies after an app reload — a documented upstream constraint, not a gap in this implementation; the language screen tells the user to restart | Revisit if a "detect device locale on first launch" default is wanted instead of always defaulting to `en` |
| 10 | Public REST API (tenant API keys) | Model exists (`ApiKey`), controller role out per Phase 7 | Phase 7 |
| 11 | Tenant creation on registration | `POST /auth/register` requires `orgName` and always creates a brand-new tenant with the caller as `OWNER`; joining an existing org only happens via an invite (not yet built, Phase 1.6) | If self-serve "join an existing org by domain" is wanted, revisit |
| 12 | CASL permission matrix (Phase 1.4) | Exact per-role grants aren't specified anywhere in `prompt.md`, so a conservative matrix was implemented in `apps/api/src/rbac/ability.factory.ts`: OWNER/ADMIN manage everything (ADMIN cannot delete the Tenant), DESIGNER/FINANCE/ANALYST/SUPPORT get read-most plus one domain-specific write, MEMBER (consumer mode) only reads Product/VideoContent | Replace with the real matrix once product defines it; CASL only gates by resource *type*, row-level scoping stays in TenantScopedRepository + RLS |
| 13 | `User` row visibility under RLS | Conservative default: a user is visible to itself always, and to any caller who shares an active membership with them inside the caller's *current* tenant context (`self_or_tenant_peer` policy, `infra/db/rls.sql`) — no cross-tenant user lookups | Revisit only if a legitimate cross-tenant feature needs it (should be rare/never) |
| 14 | Refresh-token reuse response | On detecting a reused (already-rotated) refresh token, ALL of that user's sessions are revoked immediately (assume theft) rather than just the one session, and an `AuditLog` row (`session.reuse_detected`) is written | If this proves too aggressive for legitimate double-submit races (e.g. two tabs), consider a short grace window before Phase 2 |
| 15 | `TenantContextGuard` tenant selection | Reads an optional `x-tenant-id` header to support a future org switcher; falls back to the caller's earliest active membership when absent | **Resolved in the Phase 1 follow-up pass** — `apps/web`'s `OmniSellClient` now sends the real header from the org switcher's selected tenant (`session-context.tsx`); `apps/admin` doesn't need one (platform-admin routes aren't tenant-scoped) |
| 16 | Invite acceptance without an existing account | Conservative default: `POST /v1/invites/accept` requires the caller to already be authenticated with an account whose email matches the invite (`InviteService.accept`) — no anonymous "create an account straight from the invite token" flow. The web UI (`/invites/accept`) detects this and links to `/login?returnTo=...` instead of crashing or silently failing | If self-serve "accept the invite and create your account in one step" is wanted, build a combined accept+register endpoint — tracked as `docs/DEBT.md` 1-D12 |
| 17 | OAuth account linking / auto-provisioning | Conservative default (`OAuthService.linkOrCreateUser`, unreachable without real credentials — see 1-D2): an OAuth profile whose email matches an existing `User` auto-links the new `OAuthAccount` to it (the provider already verified that email); a profile with no matching account gets a brand-new personal `Tenant` (named from the email's local part) and becomes its `OWNER`, mirroring email/password `register()`'s no-org-yet path | Revisit once product defines real org-joining semantics for OAuth sign-ups (e.g. "join an existing org by verified domain") |
| 18 | Platform-admin modeling (`User.isPlatformAdmin`) | Conservative, minimal default per prompt.md Phase 1.8's own instruction ("reuse JWT sessions with an admin-role check... do not invent a parallel auth system"): a single boolean column on `User`, checked by `AdminOnlyGuard`/`AdminService`, rather than a separate `AdminUser`/`AdminRole` model with its own role hierarchy (SUPER_ADMIN vs scoped support roles, per featureslist.md §0.2's admin-roles mention) | Replace with a real `AdminRole` hierarchy once `/admin`'s scoped-permission needs (e.g. Support Desk agents who shouldn't see Billing) are defined |
| 19 | `Blueprint` tenant-scoping (Phase 2) | prompt.md's data-model tree places `Blueprint (provider catalog cache)` directly under `Tenant`, alongside `Asset`/`Product`/etc. — read literally, that means each tenant gets its OWN copy of a provider's catalog rather than one shared platform-wide cache all tenants read from. Implemented literally: `Blueprint`/`BlueprintVariant` carry `tenantId`, are RLS-isolated per tenant like every other Phase 2 table, and are hand-seeded per-tenant in `prisma/seed.ts` (docs/DEBT.md 0-D8). The alternative (a single global cache, like `ConnectorDefinition`) would mean every tenant automatically sees the exact same Printful/Printify catalog with no per-tenant divergence possible — arguably more realistic for a literal "provider catalog," but prompt.md's own tree placement is the stronger signal here, and duplicating a modest hand-seeded catalog per tenant costs nothing today | Revisit in Phase 3 once real connector sync exists: if every tenant should always see identical blueprint data, migrate to a global cache (`ConnectorDefinition`-style) with a tenant-scoped "enabled for this tenant" join table instead of a full per-tenant copy |
| 20 | Product archive dependency guard body (Phase 2, featureslist.md 3.12) | `Listing` (Publishing) doesn't exist until Phase 4, so `ProductsService.assertNoLiveDependencies` is implemented as an empty, real (not faked) method with the exact signature (`tenantId`, `productId`) Phase 4's real check will need, called unconditionally from `archive()` today | Fill in the real `prisma.listing.count(...)` check once `Listing` exists (Phase 4) — tracked as `docs/DEBT.md` 2-D8 |
| 21 | Web asset upload default mode (Phase 2, featureslist.md 2.1/2.2) | `PRESIGNED` (direct browser-to-S3 PUT) is the real production path but cannot be verified end-to-end without a live MinIO/S3 endpoint (absent in this sandbox); `RESUMABLE` (chunked through the API's own endpoints) is the only mode that can actually complete here since its storage is API-local disk. Conservative default: the web Asset Library UI (`apps/web/lib/resumable-upload.ts`) always uses `RESUMABLE` | Switch the default to `PRESIGNED` (or offer a toggle) once a live S3-compatible endpoint exists to verify against — tracked as `docs/DEBT.md` 2-D1/2-D14 |
| 22 | tus wire-protocol fidelity (Phase 2, featureslist.md 2.2) | The real tus protocol PATCHes raw `application/offset+octet-stream` bytes; this phase's resumable-chunk endpoint instead accepts a JSON envelope (`{offsetBytes, chunkBase64}`) so it works through Nest's default JSON body parser without extra raw-body middleware. Conservative default: keep the offset-tracking *semantics* real (a mismatched offset is rejected exactly like a real tus server would) while scoping down the wire *format*, since this repo's own web/mobile clients are the only consumers this phase | Move to a real tus server library (e.g. `@tus/server`) or raw-body middleware if third-party tus-client interoperability is ever required — tracked as `docs/DEBT.md` 2-D3 |
| 23 | Connector registry `status` for the four Phase 3 adapters (prompt.md Phase 3 / api-registration.md §7) | `CONNECTOR_STATUSES` offers `ACTIVE\|BETA\|GATED\|UNVERIFIED\|RETIRED` but prompt.md never says which one a connector with real live-doc-confirmed code but no completed human sandbox-call + legal sign-off should get. Conservative default: `'BETA'` — visible to users (unlike `UNVERIFIED`), but honestly short of the full verification protocol. `verifiedBy` on these four seed rows names this build pass explicitly, not a person, rather than fabricating a human sign-off | Flip to `'ACTIVE'` only after api-registration.md §7's full checklist (real sandbox account + call + legal sign-off) completes for each connector |
| 24 | Two Tier C/D rows exist with no adapter code (prompt.md Phase 3 task 3.1's "may create registry rows... to prove the compile-time gate") | Seeded exactly two: Redbubble (Tier C, real platform, `canAutomate: false`) and SunFrog (Tier D, per api-registration.md §5's own dead-platform list). Conservative default: the SMALLEST set that exercises both the Tier-C `publish()` compile-time boundary AND the admin quarantine screen against real (non-fabricated) registry data, clearly documented in code comments and docs/CONNECTORS.md as boundary-proof only, not shipped connectors | Do not grow this list — the wave-2/Tier B/D lists from featureslist.md §16 are later phases' scope |
| 25 | Per-connector rate limits on the seed data (implentationplanphase.md task 3.5) | None of the four providers' docs pages fetched this pass stated an exact numeric rate limit in the retrieved excerpt. Conservative default: reasonable, documented ESTIMATES (120/100/60/60 requests per 60s for Printful/Printify/Gelato/Prodigi) rather than inventing false precision or leaving the field empty (the schema requires a `ConnectorRateLimitConfig`) | Confirm each provider's actual published numeric rate limit and update the seed — tracked as `docs/DEBT.md` 3-D7 |
| 26 | What "key rotation" means for the credential vault (prompt.md constraint #3 / task 3.2) | Implemented as re-wrapping a tenant's DEK under a new `KMS_MASTER_KEY` value (`CredentialVaultService.rotateMasterKeyWrap`) — every `Credential` row stays byte-for-byte unchanged, only the one `TenantDataKey.wrappedDek` row changes. This is the standard, sufficient meaning of "rotation" in an envelope-encryption scheme (re-encrypting every credential under a brand-new DEK would be a DIFFERENT operation — full re-keying — not implemented this phase since nothing requires it yet) | Add a full DEK-rotation (not just master-key-wrap-rotation) path if a future requirement (e.g. a suspected DEK compromise, not just a KMS key rotation) needs it |
| 27 | Provider OAuth callback route has no `JwtAuthGuard` (implentationplanphase.md task 3.3) | `GET /v1/oauth/callback/:slug` is reached by a full-page browser redirect FROM the provider, which carries no OmniSell Authorization header — the single-use, time-limited `state` token (minted while the caller WAS authenticated, in `ConnectionsService.startOAuth`) is the trust anchor instead, mirroring Phase 1's SSO callback's identical trust model for the identical reason. Conservative default: rely on state validation + single-use consumption + the connector-slug allowlist check, not a session header that can't exist at this point in the flow | Revisit only if a future requirement needs the callback itself to know which OmniSell session initiated it beyond what the state row already encodes (it currently carries `tenantId`/`connectionId`, which is sufficient for every check performed) |
| 28 | Disconnect retention choice (`PURGE` vs `KEEP_ORPHAN`, featureslist.md 4.17) when no `Listing` model exists yet | `ConnectionsService.disconnect` records the caller's choice (`Connection.retentionChoice`) and deactivates credentials, but there is nothing to actually purge yet — `Listing` doesn't exist until Phase 4, the exact same forward-looking-seam pattern as Phase 2's `ProductsService.assertNoLiveDependencies` (`docs/DEBT.md` 2-D8). Conservative default: record the real choice now so Phase 4 doesn't need a schema change, implement the real purge/orphan-preservation logic once `Listing` exists | Phase 4: once `Listing` exists, make `retention: 'PURGE'` actually delete/detach listings created through the disconnected connection |
| 29 | Admin registry "sandbox toggle" (implentationplanphase.md task 3.12) — ambiguous between two real things | Interpreted as toggling the REGISTRY-LEVEL `capabilities.supportsSandbox` flag (does this connector type offer a sandbox environment at all) — distinct from the PER-CONNECTION `Connection.sandbox` boolean a user sets in the connection wizard (does THIS tenant's connection use the sandbox). Both exist; the admin screen's toggle only touches the registry-level flag, which is the one admins actually own | If this reads as confusing in practice, rename the admin button to `capabilities.supportsSandbox` explicitly rather than the generic "sandbox toggle" wording |
| 30 | `Listing.status` state machine vs. featureslist.md 5.6/5.10/4.12's job/approval/export-pack language (Phase 4 task 4.1) | implentationplanphase.md's Phase 4 entry gives an EXACT 6-value state machine (`DRAFT -> PENDING -> QUEUED -> LIVE / REJECTED / ERROR`) but never says how approval (5.10) or a Tier C "confirm manual upload" step (4.12/5.15) map onto it. Conservative default implemented: `Listing.status` stays EXACTLY those six values, never gaining an approval- or export-pack-specific state. Approval is tracked on an ORTHOGONAL `Listing.approvalStatus` field (`NONE\|SUBMITTED\|APPROVED\|REJECTED`) — `submitForApproval` moves `status` to `PENDING` (approval-orthogonal "in the pipeline" meaning) while setting `approvalStatus: SUBMITTED`; an `APPROVED` decision only changes `approvalStatus` (status stays whatever it was, ready to publish); a `REJECTED` decision changes BOTH `approvalStatus` and `status` to `REJECTED` (a rejected listing is genuinely terminal). A Tier C listing that has generated its Export Pack sits in `QUEUED` ("queued for the human's manual action") until the user confirms upload, at which point `ExportPackGeneratorService.confirm()` moves it to `LIVE` — the same terminal state an automated publish reaches, via a different path | Revisit if product wants a richer status vocabulary (e.g. a distinct `AWAITING_MANUAL_UPLOAD` value) instead of overloading `QUEUED` for both "in a BullMQ queue" and "waiting on a human" |
| 31 | brb.md §4's persona table names a `MANAGER` role ("Studio Manager (Omar)... approve listings") for the approval workflow (featureslist.md 5.10), but Phase 1's actual 7-role `OrgRole` enum (`OWNER\|ADMIN\|DESIGNER\|FINANCE\|ANALYST\|SUPPORT\|MEMBER`, prompt.md Phase 1.4 / FR-02's "7 org-level roles") has no `MANAGER` value at all | Conservative default: `ListingsController`'s approval-decision endpoint is CASL-gated to `ability.can('update', 'Listing')`, granted to `OWNER`/`ADMIN` (both already `manage: 'all'` per docs/OPEN_QUESTIONS.md #12) — the closest existing roles to "Studio Manager". `DESIGNER` gets `create`/`update`/`delete` on `Listing` (composing/submitting), matching brb.md's "DESIGNER submits" half exactly | Replace with a real `MANAGER` role (or a scoped permission) if product formalizes the 7-role matrix into 8, or if `OWNER`/`ADMIN` approving is deemed too broad in practice |
| 32 | Literal colon-suffixed route paths (`POST /listings:publish`, `:dry-run`, `:bulk`) — prompt.md's API surface writes these with a literal colon, Google-API-style pseudo-RPC verb suffix | Confirmed safe and already load-bearing in this codebase: Phase 2's `products/:id/variants:bulk` already relies on the exact same Express/`path-to-regexp` behaviour (a `:` only starts a route-param capture when it immediately follows a `/`; mid-segment it's a literal character) — proven again this phase via a real `next dev`-equivalent boot + curl (`Mapped {/v1/listings:dry-run, POST} route`). Implemented literally, matching prompt.md's API surface verbatim rather than substituting a slash | None open — this is confirmed working, not a risk |
| 33 | "Scheduled" and "Rejected/Errors" sidebar views (featureslist.md's nav tree) aren't real `LISTING_STATUSES` values on their own — "Scheduled" is `DRAFT` with `scheduledAt` set; "Rejected/Errors" is the union of two real statuses | Conservative default: added a dedicated `view` query param (`'REJECTED_OR_ERROR' \| 'SCHEDULED'`) to `listListingsQuerySchema` alongside `status`, rather than overloading `status` with fake values. `ListingsService.list` translates `view` into the real filter (`statuses: ['REJECTED','ERROR']` or `scheduledOnly: true` + `status: 'DRAFT'`) | Revisit if more such "virtual" views accumulate — a small view-registry might read better than a growing union type at that point |
| 34 | Bulk `REPRICE`/`RETAG` (task 4.8): should they push the change to an already-LIVE channel via `adapter.update()`, or stay local to OmniSell? | Conservative default (docs/DEBT.md 4-D7): LOCAL-ONLY this phase — updates `Listing`/`ListingVariant` rows, does not call `adapter.update()`. Reasoning: an unreviewed bulk price/tag change silently pushed live to N channels at once is a bigger blast radius than this phase's time budget allows building (and testing) safely; `RESYNC`/drift detection exists precisely to surface the resulting local-vs-channel gap afterwards, and a human can force-push deliberately via the drift UI's "Force-push" action (which DOES call the real publish/update path) | Wire `REPRICE`/`RETAG` to also enqueue a real `update`-kind job once product confirms this blast radius is acceptable |
| 35 | Redbubble's registry-row status: Phase 3 deliberately seeded it `UNVERIFIED` ("boundary-proof only, not shipped" — docs/OPEN_QUESTIONS.md #24); Phase 4's own exit criterion needs "a Redbubble Export Pack a user can actually follow to upload manually" | Promoted this phase to `status: 'BETA'` with a real, sourced `fieldSpec` (docs/DEBT.md 4-D5) — the same honest-but-visible standard the four Tier A adapters use, superseding #24's "hide it" choice specifically for Redbubble (SunFrog, the Tier D row, stays hidden/`UNVERIFIED` — that choice is unchanged) | Flip to `'ACTIVE'` only after a human independently re-verifies the exact title/description character caps (currently estimated) against Redbubble's own current help-center pages |
| 36 | `ConnectorAdapter.fetchListingState` (new optional SDK method this phase, for drift detection, task 4.13) — implement it against all four real adapters now, or leave it a real-but-unimplemented seam? | Conservative default: left unimplemented for all four (docs/DEBT.md 4-D3) — no live-doc-confirmed single-listing "get" endpoint was independently re-verified this pass for any of the four providers, and prompt.md constraint #2 forbids guessing one. `DriftDetectionService.check()` returns an honest `{ supported: false }` rather than a fabricated comparison; the comparison logic itself is proven against a fake adapter in `test/drift-detection.service.test.ts` | Implement per-adapter once each provider's single-listing "get" endpoint is confirmed live (same verification protocol as api-registration.md §7) |
| 37 | Where do the Phase 4.5 admin surfaces (Point Rules, Video moderation, Fraud review queue, Point adjustment — docs/points-extension.md §10.3) live: the tenant-scoped web app (`apps/web`, RBAC-gated) or the platform console (`apps/admin`, `AdminOnlyGuard`-gated)? points-extension.md's own wording ("Admin — new sections") doesn't say | Implemented as tenant-scoped web pages (`apps/web/app/(shell)/points/*`), RBAC-gated via `PoliciesGuard`/`CheckPolicies` — NOT under `apps/admin`. Reasoning: `TenantPointSettings`/`PointEarningRule` are per-tenant business configuration, not platform-wide policy, and Phase 0/1's own RBAC scaffold already anticipated this — `ability.factory.ts` already granted DESIGNER write access to `VideoContent`, FINANCE write access to `PointEarningRule`, and SUPPORT read/adjust access to `Wallet`/`PointTransaction` before this phase ever touched those files, which is a strong prior signal for "tenant RBAC", not "platform admin" | Move to `apps/admin` instead if product decides these should be a platform-operator concern (e.g. to prevent a tenant's own OWNER from lowering the redemption floor to drain their own points liability) rather than a tenant self-service setting |
| 38 | Minimal `LedgerEntry`/`LedgerLine` schema shape (prompt.md "CONSUMER MODE" section says the base entities "exist in the base tree" but gives no field list — only `LedgerEntry ─── LedgerLine (debit/credit, must balance)`) | Implemented the smallest real double-entry primitive that satisfies "must balance": `LedgerEntry{id, tenantId, occurredAt, memo, sourceType, sourceId, createdAt}` + `LedgerLine{id, ledgerEntryId, tenantId, accountCode, direction(DEBIT\|CREDIT), amountMinor(always non-negative), currencyCode, createdAt}`, with the DEBIT=CREDIT assertion enforced IN CODE (`LedgerService.postBalancedEntry`) before any write, not just by convention. Two account codes exist so far (`points_liability`, `sales_discounts`) — the minimum a redemption discount/refund actually touches | Phase 6 (Finance/Ledger/Tax) extends this table (FX, fee decomposition, `Payout`/`PayoutLine` linkage, period locks, a real chart of accounts) rather than replacing it — the shape here should not need a breaking change, only new columns/rows |
| 39 | `VideoWatch`'s exact column set beyond points-extension.md §6's own "differences from the informal list" callout (which already added `heartbeatCount`/`maxGapSeconds`) — running the real §8.1 heartbeat-gap-and-acceleration protocol needs to know the PREVIOUS heartbeat's server-received time and the client's previous claimed position, neither of which the documented column list provides | Added two more implementation columns not named in §6: `lastHeartbeatAt` (server-received time of the previous heartbeat) and `lastWatchPosition` (client-claimed position at the previous heartbeat), plus `fraudSignals` (a `Json` array of signal codes, for the admin fraud-queue view) — all three are transient/derived-use columns, not new domain concepts, and are documented inline in the schema with the exact reasoning | None open — these are additive, non-breaking columns; revisit only if a genuinely different heartbeat-gap algorithm needs different derived state |
| 40 | `VideoContent` creation path (§9.4: "Uploads reuse the base presigned + tus pipeline; `url` is stored, `durationSeconds` is derived... by a server-side probe") — the base pipeline's "complete" step creates an `Asset` row, which isn't what a `VideoContent` row is | Implemented two real, server-probed sources instead of forcing video bytes through the `Asset` model: (1) `uploadSessionId` — reuses Phase 2's `AssetUploadSessionRepository`/`ResumableUploadStorage` scratch storage directly (bytes land the same way an Asset's would, just never promoted to an `Asset` row), probed via a genuinely working `ffprobe`; (2) an external `url` — the server downloads (bounded, timed-out) and probes the same way. Both paths make `durationSeconds` server-derived, never client-claimed, in either case | If a unified upload experience across Assets and Videos becomes a real requirement, consider adding a `VIDEO` `AssetKind` instead of the two parallel paths implemented here |
| 41 | Redemption's `subtotalMinor` when no `Order` model exists yet (Phase 5 hasn't shipped) | **Resolved this pass (Phase 5)** — `RedemptionService.computeMath` now uses the real `Order.subtotalMinor`/`currency` when a caller supplies `orderId`; the single-product path stays available for a pre-Order preview/confirm. See docs/DEBT.md 4.5-D6 (closed) | None open |
| 42 | Inbound webhook routing (task 5.1) — prompt.md's literal `POST /hooks/:slug` has no tenant/connection discriminator, but a real webhook MUST resolve to exactly one tenant's one connection to decrypt the right credential and verify the right signature | Conservative default: extended to `POST /hooks/:slug/:connectionId` (docs/DEBT.md 5-D2) — `connectionId` is embedded in the per-tenant callback URL the tenant registers with the provider, resolved via a new, deliberately untenanted `ConnectionRepository.findTenantIdById` lookup (the one legitimate exception to "every repository method injects tenantId", mirroring how `ConnectorOAuthState` resolves tenant from its own state token rather than a caller-supplied header, docs/OPEN_QUESTIONS.md #27) | Revisit only if a future requirement needs the literal `/hooks/:slug` path with no discriminator — would require a different trust model entirely (e.g. a shared webhook secret per connector, not per connection) |
| 43 | Does a PARTIAL refund proportionally restore SOME of a redemption's points, or only a FULL refund/cancellation? Neither prompt.md nor points-extension.md addresses partial refunds interacting with points at all | Conservative default: only a refund that reaches or exceeds `Order.totalMinor` (i.e. becomes effectively a full refund) transitions the order to `REFUNDED`, which is the only path that fires `RedemptionService.refund` (docs/DEBT.md 5-D10). A smaller partial refund leaves the redemption's points spent | Revisit if product wants proportional points restoration on partial refunds — would need a new, explicitly-defined proportional-restoration formula, not implemented here |
| 44 | Digital-product entitlement auto-granting (task 5.10/7.4) — should a CHANNEL-ingested order (e.g. an Etsy digital download) automatically grant an OmniSell `Entitlement`? | Conservative default: NO — auto-granting only happens from `OrdersService.createManualOrder` (a digital-only sale OmniSell itself creates, where it already knows the buyer AND the `DigitalProduct` mapping at creation time). A channel-ingested `OrderItem` has no SKU-to-`DigitalProduct` mapping in this schema, so guessing one would risk granting access to the wrong file — `EntitlementService`'s own doc comment documents this explicitly, and `POST /entitlements` exists for a human to grant one manually once they've identified the right mapping | Add a per-tenant SKU→DigitalProduct mapping table if automatic entitlement-granting from channel orders becomes a real requirement |
| 45 | SLA windows per exception type (featureslist.md 6.11 — "SLA timers") — no source document specifies exact hours per `ORDER_EXCEPTION_TYPES` value | Conservative default: `DEFAULT_SLA_HOURS` (`sla.util.ts`) — `PRINT_REJECT`: 12h, `ADDRESS_INVALID`/`PAYMENT_HOLD`: 24h, `OUT_OF_STOCK`: 48h, `CUSTOMS`: 72h — reasonable estimates ordered by "how fast does this typically need a human", not a confirmed product/ops requirement. No per-tenant SLA override table exists yet (kept as a single global config to match this phase's time budget) | Add a real per-tenant SLA-configuration table/admin UI once product specifies real target windows |
| 46 | BOGO coupon modeling (featureslist.md 7.6 — "%/fixed/BOGO") — a BOGO discount's exact amount depends on WHICH cart item becomes free, a cart-level decision the coupon engine alone can't make | Conservative default: `CouponService` treats BOGO identically to FIXED for its own bookkeeping (usage caps/expiry/channel/customer-limit enforcement) — `Coupon.valueMinor` is the flat discount amount a BOGO coupon resolves to; identifying WHICH order item is "the free one" is left to the checkout/cart caller, documented explicitly in `CouponService`'s own doc comment | Revisit if a real BOGO checkout flow needs the coupon engine itself to pick the cheapest/specific eligible item rather than trusting the caller |
| 47 | Web sidebar sub-navigation scope for Orders/Digital Products (nav-data.ts already listed 11 sub-routes; this phase's time budget) | Conservative default: built 4 of the 11 as real pages (`/orders`, `/orders/exceptions`, `/digital/files`, `/digital/coupons`) plus the order detail drawer (`/orders/[id]`) — the remaining 7 status-filtered/sub-feature views (`/orders/unfulfilled`, `/orders/in-production`, `/orders/shipped`, `/orders/returns`, `/digital/licences`, `/digital/delivery-log`, `/digital/entitlements`) still fall through to the `(shell)/[...slug]` coming-soon catch-all — every one of their BACKEND endpoints is real and covered by the same controllers/services/tests as the built pages (docs/DEBT.md 5-D8) | Build the remaining thin status-filtered views (each mirrors `ListingsListView`'s pattern of one shared component + a status/view prop) once prioritised |